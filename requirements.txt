flask>=3.0.0
flask-compress>=1.14
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
gunicorn>=21.2.0
python-dotenv>=1.0.0
pypdf>=4.0.0
# Notation: engraves MusicXML -> SVG and transposes it (app/notation.py).
verovio>=4.0.0
# PyMuPDF and anthropic were dropped 2026-07-23 with the vision transcriber.
# Charts are now imported as MusicXML from Soundslice, so nothing here
# rasterises pages or calls an LLM — which also retires PyMuPDF's AGPL-3.0
# licence problem (it would have needed a commercial licence to ship publicly).
